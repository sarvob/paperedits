node_modules/
dist/
*.tsbuildinfo
.DS_Store
.cache/
# Local model + media artifacts must never be committed
*.mp4
*.mov
*.mkv
*.wav
*.aiff
models/
samples/
.pve-cache/
# Secrets never touch the repo (keys live in the OS keychain)
.env
.env.local
